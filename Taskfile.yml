version: '3'
includes:
  image-scraper:
    dir: ./apps/fab-image-scraper/
    taskfile: ./apps/fab-image-scraper/Taskfile.yml
tasks:
  build:
    cmds:
      - task: image-scraper:build
  run-scraper:
    cmds:
      - task: image-scraper:run
